расширение для отслеживания state, dispatch, actions,
(diff) изменений которые происходят после действий

Так-же если нажимать на события слевой стороны, можно отслеживать что именно было изменено в определенный момент (JUMP).